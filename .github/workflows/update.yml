name: Update sources

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-tags: true
      - uses: DeterminateSystems/nix-installer-action@v22
      - run: ./.github/update.sh
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Test build
        run: nix build .#default --print-build-logs
      - id: version
        run: |
          version=$(jq -r '."x86_64-linux".version' sources.json)
          echo "version=$version" >> "$GITHUB_OUTPUT"
          [ -n "$(git tag -l "v$version")" ] || echo "tag=v$version" >> "$GITHUB_OUTPUT"
      - uses: stefanzweifel/git-auto-commit-action@v7
        with:
          commit_message: "chore: update sources to ${{ steps.version.outputs.version }}"
          file_pattern: sources.json
          tagging_message: ${{ steps.version.outputs.tag }}
